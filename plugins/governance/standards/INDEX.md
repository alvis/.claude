# Governance standards index

Select applicable standards for authored artifacts from the table below. The protocol that governs selection, scanning, and rule lookup is `essential:directions/standards.md`; this file only says which standard applies.

| Applies to | Standard |
| --- | --- |
| Authored agents, skills, standards, subagent assignments, and reports | `governance:standards/authoring/` |
| Artifacts that dispatch subagents and delegated execution | `governance:standards/delegation/` |

Governance owns authoring invariants and templates, not standards governing its own work: a standard being authored is the target artifact, not an implicit standard governing unrelated governance work. Do not select a standard from an undeclared plugin.
