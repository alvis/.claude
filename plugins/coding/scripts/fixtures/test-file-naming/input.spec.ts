// compliant: this file uses the correct `.spec.ts` extension
import { describe, it, expect } from "vitest";

describe("fn:placeholder", () => {
  it("should return one", () => {
    expect(1).toBe(1);
  });
});
