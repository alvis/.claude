=== Time/measurement identifier without unit suffix (NAM-CORE-04) ===

input.ts:3  const timeout = 5000;
       1: export function configure(): void {
       2:   // violation: bare `timeout` hides its unit
  >    3:   const timeout = 5000;
       4: 
       5:   // violation: bare `delay` hides its unit
       6:   const delay = 200;
       7: 
       8:   // compliant: unit suffix makes the value unambiguous
       9:   const connectionTimeoutMs = 5000;
      10:   const retryDelaySeconds = 2;
      11: 
      12:   void timeout;
      13:   void delay;

  --- next match ---

input.ts:6  const delay = 200;
       1: export function configure(): void {
       2:   // violation: bare `timeout` hides its unit
       3:   const timeout = 5000;
       4: 
       5:   // violation: bare `delay` hides its unit
  >    6:   const delay = 200;
       7: 
       8:   // compliant: unit suffix makes the value unambiguous
       9:   const connectionTimeoutMs = 5000;
      10:   const retryDelaySeconds = 2;
      11: 
      12:   void timeout;
      13:   void delay;
      14:   void connectionTimeoutMs;
      15:   void retryDelaySeconds;
      16: }

=== Summary ===
  unit-suffix: 2 matches in 1 files
