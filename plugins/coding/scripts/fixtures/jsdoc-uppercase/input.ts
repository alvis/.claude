/**
 * Computes the running total.
 * this lowercase line is fine.
 */
export function compute(): number {
  return 1;
}

/** API client for the upstream service. */
export const client = {};

/** ReactNode is PascalCase and must not flag. */
export type Marker = string;

/**
 * @param userId the unique identifier
 * @returns The decoded value
 * @example
 * const x = compute();
 */
export function decode(userId: string): number {
  return userId.length;
}
