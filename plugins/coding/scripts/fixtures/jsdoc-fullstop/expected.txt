=== JSDoc: trailing period ===

input.ts:2  * Ends with a period.
       1: /**
  >    2:  * Ends with a period.
       3:  * does not end with punctuation
       4:  * Resolves after a delay...
       5:  * Uses e.g. exponential backoff.
       6:  */
       7: export function delay(): void {}
       8: 
       9: /** One-line ending with period. */
      10: export const flag = true;

  --- next match ---

input.ts:5  * Uses e.g. exponential backoff.
       1: /**
       2:  * Ends with a period.
       3:  * does not end with punctuation
       4:  * Resolves after a delay...
  >    5:  * Uses e.g. exponential backoff.
       6:  */
       7: export function delay(): void {}
       8: 
       9: /** One-line ending with period. */
      10: export const flag = true;

  --- next match ---

input.ts:9  /** One-line ending with period. */
       4:  * Resolves after a delay...
       5:  * Uses e.g. exponential backoff.
       6:  */
       7: export function delay(): void {}
       8: 
  >    9: /** One-line ending with period. */
      10: export const flag = true;

=== Summary ===
  jsdoc-fullstop: 3 matches in 1 files
