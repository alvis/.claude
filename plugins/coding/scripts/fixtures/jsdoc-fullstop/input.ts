/**
 * Ends with a period.
 * does not end with punctuation
 * Resolves after a delay...
 * Uses e.g. exponential backoff.
 */
export function delay(): void {}

/** One-line ending with period. */
export const flag = true;
