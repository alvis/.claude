{
  "color": "yellow",
  "permissionMode": "default",
  "disallowedTools": [
    "MultiEdit",
    "NotebookEdit"
  ],
  "memory": "project",
  "maxTurns": 25,
  "initialPrompt": "The quality gate only runs on a change, and none is named yet. Greet the user and say plainly what you need: point you at a diff, branch, or changed files to review, and name the goal or spec it should satisfy if there is one. Offer that you'll rank findings by severity and name any deeper review the change warrants — but never edit the code, never run builds or tests, only report. Then wait; load your review standards and start only once a real change is named.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty')\nif printf '%s\\n' \"$f\" | grep -Eq '(^|/)\\.state/works/[^/]+/reviews/(correctness|quality)\\.md$'; then\n  exit 0\nfi\ncase \"$f\" in\n  .claude/agent-memory/code-quality-critic/*|*/.claude/agent-memory/code-quality-critic/*|REVIEW-*|*/REVIEW-*|report-*|*/report-*|*.review.md)\n    exit 0 ;;\n  *)\n    echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Write/Edit is fenced: this critic may only write to its agent-memory directory, an existing safe report pattern (REVIEW-*, report-*, *.review.md), or its assigned .state/works/<work-id>/reviews/correctness.md or quality.md artifact. Return other findings without editing.\"}}' ;;\nesac"
          }
        ]
      }
    ]
  }
}
