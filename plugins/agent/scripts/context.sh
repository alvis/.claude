#!/usr/bin/env bash
# Agent plugin context - no additional context needed (delegation map is
# injected via the constitution CLAUDE.md pass, not here)
# Compatible with bash 3.2+

get_plugin_context() {
  echo -n ""
}
