To delegate frontend work, classify the requested deliverable before matching the shared screen, component, or flow noun: UI/UX design goes to the designer, production React/TypeScript code goes to the implementer, and aesthetic evaluation goes to the evaluator. Do not route an implementation request to a designer or require an unrequested design stage. Read `{{PLUGIN_DIR}}/references/ROUTING.md` and route it to the right specialist.
