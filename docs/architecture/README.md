# Architecture

- [Harness projections](harness-projections.md) owns the relationship between canonical plugin sources, native harness manifests, compatibility consumers, and the OpenCode V1 runtime adapter.

`ARCHITECTURE.md` at the repository root documents the older scanner subsystem; it is not authoritative for harness projection behavior.
